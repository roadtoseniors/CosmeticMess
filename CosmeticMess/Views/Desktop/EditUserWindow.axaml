<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="CosmeticMess.Views.Desktop.UserEditWindow"
        Title="Пользователь" Width="380" Height="480"
        Background="#f6f6f6" FontFamily="Georgia" Foreground="#8785a2"
        CanResize="False" WindowStartupLocation="CenterOwner">
    <StackPanel Spacing="12" Margin="24">
        <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold"/>
        <StackPanel Spacing="4">
            <TextBlock Text="Имя" FontSize="13" Opacity="0.7"/>
            <TextBox x:Name="NameBox" FontSize="15"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock Text="Фамилия" FontSize="13" Opacity="0.7"/>
            <TextBox x:Name="LastNameBox" FontSize="15"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock Text="Телефон" FontSize="13" Opacity="0.7"/>
            <TextBox x:Name="PhoneBox" FontSize="15"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock Text="Возраст" FontSize="13" Opacity="0.7"/>
            <NumericUpDown x:Name="AgeBox" Minimum="0" Maximum="120" FontSize="15"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock Text="Роль" FontSize="13" Opacity="0.7"/>
            <ComboBox x:Name="RoleBox" ItemsSource="{Binding Roles}"
                      HorizontalAlignment="Stretch" FontSize="15">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock Text="Логин" FontSize="13" Opacity="0.7"/>
            <TextBox x:Name="LoginBox" FontSize="15"/>
        </StackPanel>
        <StackPanel Spacing="4">
            <TextBlock Text="Пароль" FontSize="13" Opacity="0.7"/>
            <TextBox x:Name="PasswordBox" PasswordChar="*" FontSize="15"/>
        </StackPanel>
        <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="13" IsVisible="False"/>
        <Button Content="Сохранить" Background="#ffc7c7" Foreground="#8785a2" FontFamily="Georgia" FontSize="18" HorizontalAlignment="Stretch" Height="44" x:Name="Save" Click="Save_OnClick"/>
    </StackPanel>
</Window>